<Window x:Class="RAT_WPF.NetworkObject_UI.UpdateInterfaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RAT_WPF.NetworkObject_UI"
        mc:Ignorable="d"
        Title="Interface" Width="540" MinWidth="420" MinHeight="280"
        SizeToContent="Height" MaxHeight="760"
        Icon="{StaticResource AppLogo}"
        Background="{DynamicResource Brush.Background}"
        TextElement.Foreground="{DynamicResource Brush.Text}"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">
    <!--KI start (Claude Opus 4.8, prompt 1/3): docked button bar + scroll so Ok/Cancel always fit-->
    <Border Margin="16" Background="{DynamicResource Brush.Surface}" CornerRadius="8" Padding="18">
        <DockPanel>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                <Button Content="Cancel" Style="{StaticResource SubtleButton}" Click="Cancel_Click"/>
                <Button Content="Ok" Style="{StaticResource ModernButton}" Click="Button_Click"/>
            </StackPanel>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Network interface" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,2"/>
                    <TextBlock Text="Only the name is required — IP settings are optional."
                               Foreground="{DynamicResource Brush.TextMuted}" FontSize="11" Margin="0,0,0,8"/>
                    <TextBlock Text="Interface name *" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="InterfaceNameTextBox" Style="{StaticResource ModernTextBox}"/>
                    <TextBlock Text="IPv4 (optional)" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="Ipv4TextBox" Style="{StaticResource ModernTextBox}"/>
                    <TextBlock Text="IPv4 subnet mask (optional)" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="Ipv4MaskTextBox" Style="{StaticResource ModernTextBox}"/>
                    <TextBlock Text="IPv4 gateway (optional)" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="Ipv4GatewayTextBox" Style="{StaticResource ModernTextBox}"/>
                    <TextBlock Text="IPv6 (optional)" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="Ipv6TextBox" Style="{StaticResource ModernTextBox}"/>
                    <TextBlock Text="IPv6 prefix length (optional)" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="Ipv6PrefixTextBox" Style="{StaticResource ModernTextBox}" Text=""/>
                    <TextBlock Text="IPv6 gateway (optional)" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="Ipv6GatewayTextBox" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
    <!--KI end-->
</Window>
