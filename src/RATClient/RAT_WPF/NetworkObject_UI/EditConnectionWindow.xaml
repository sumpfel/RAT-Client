<Window x:Class="RAT_WPF.NetworkObject_UI.EditConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RAT_WPF.NetworkObject_UI"
        mc:Ignorable="d"
        Title="Edit cable" Width="440" MinWidth="360" SizeToContent="Height" MaxHeight="600"
        Icon="{StaticResource AppLogo}"
        Background="{DynamicResource Brush.Background}"
        TextElement.Foreground="{DynamicResource Brush.Text}"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">
    <!--KI start (Claude Opus 4.8, prompt 25): edit a cable's name / type / speed (preset dropdown) / note-->
    <Border Margin="16" Background="{DynamicResource Brush.Surface}" CornerRadius="8" Padding="18">
        <DockPanel>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Cancel" Style="{StaticResource SubtleButton}" Click="Cancel_Click"/>
                <Button Content="Save" Style="{StaticResource ModernButton}" Click="Save_Click"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Cable / connection" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>

                <TextBlock Text="Name" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="NameBox" Style="{StaticResource ModernTextBox}"/>

                <TextBlock Text="Type" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="TypeCombo" Width="160" HorizontalAlignment="Left"
                          SelectionChanged="TypeCombo_SelectionChanged">
                    <ComboBoxItem Content="Wired"/>
                    <ComboBoxItem Content="Wireless"/>
                </ComboBox>

                <TextBlock Text="Speed" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="SpeedCombo" Width="220" HorizontalAlignment="Left"/>

                <TextBlock Text="Note" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="NoteBox" Style="{StaticResource ModernTextBox}"
                         AcceptsReturn="True" TextWrapping="Wrap" MinHeight="50"/>
            </StackPanel>
        </DockPanel>
    </Border>
    <!--KI end-->
</Window>
