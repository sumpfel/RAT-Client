<UserControl x:Class="RAT_WPF.Views.NetworkObjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RAT_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="80" x:Name="grid">
    <Grid Width="80" Height="70" MouseMove="Grid_MouseMove">
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <!-- <RowDefinition Height="15"/> -->
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Rectangle Fill="LightSeaGreen" Grid.Row="0"/>
        <TextBlock Text="{Binding Type}" Grid.Row="0"/>
        <!--<TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Background ="WhiteSmoke" Grid.Row="1"/> -->
        <Button Grid.Row="1" Content="Settings" Command="{Binding NetworkObjectOpenSettings}"/>
    </Grid>
</UserControl>
