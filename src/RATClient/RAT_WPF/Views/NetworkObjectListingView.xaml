<UserControl x:Class="RAT_WPF.Views.NetworkObjectListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RAT_WPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="410" d:DesignWidth="180">
    <!--KI start (Claude Opus 4.8, prompt 2): themed device palette with icons-->
    <Border Background="{DynamicResource Brush.Surface}">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="Devices" Style="{StaticResource Header3}" Margin="8,8,8,4"/>
            <ListView ItemsSource="{Binding NetworkObjects}" Background="Transparent" BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <EventSetter Event="MouseMove" Handler="ListviewNetworkObject_MouseMove"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="2">
                            <Image Width="28" Height="28" Margin="0,0,8,0"
                                   Source="{Binding Type, Converter={StaticResource IconKeyConverter}}"/>
                            <TextBlock Text="{Binding Type}" VerticalAlignment="Center"
                                       Foreground="{DynamicResource Brush.Text}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </DockPanel>
    </Border>
    <!--KI end-->
</UserControl>
