<Window x:Class="RAT_WPF.EditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit user" Width="460" MinWidth="380"
        SizeToContent="Height" MaxHeight="640"
        Icon="{StaticResource AppLogo}"
        Background="{DynamicResource Brush.Background}"
        TextElement.Foreground="{DynamicResource Brush.Text}"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">
    <!--KI start (Claude Opus 4.8, prompt 16): edit-user dialog. Admin-only fields (Admin / Can create) are hidden
        when the caller isn't allowed to change them (a normal user editing themselves). Empty password = unchanged.-->
    <Border Style="{StaticResource Card}" Margin="16">
        <DockPanel>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Save" Style="{StaticResource ModernButton}" Click="Save_Click"/>
                <Button Content="Cancel" Style="{StaticResource SubtleButton}" Click="Cancel_Click"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Edit user" Style="{StaticResource Header}" Margin="0,0,0,12"/>

                <TextBlock Text="Username" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="UserNameBox" Style="{StaticResource ModernTextBox}"/>

                <TextBlock Text="New password (leave empty to keep current)" Style="{StaticResource FieldLabel}"/>
                <PasswordBox x:Name="PasswordBox" Height="30" VerticalContentAlignment="Center"/>

                <StackPanel x:Name="AdminFieldsPanel" Margin="0,12,0,0">
                    <CheckBox x:Name="IsAdminBox" Content="Admin" Margin="0,0,0,6"/>
                    <CheckBox x:Name="CanCreateBox" Content="Can create devices"/>
                </StackPanel>
            </StackPanel>
        </DockPanel>
    </Border>
    <!--KI end-->
</Window>
